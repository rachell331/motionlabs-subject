import { Interface } from "readline";

const theme: { [key: string]: string } = {
  bgColor: "#f0f3f8",
  mainColor: "#333333",
  fontBlack: "#000000",
  fontWhite: "#ffffff",
};

export default theme;
